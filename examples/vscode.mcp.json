{
  "servers": {
    "recraft": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/recraft-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
